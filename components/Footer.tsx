import React from "react";

interface Props {
    name?:string
}

const Footer: React.FC<Props> = ({ name }) => {
    return (
        <div>
           sdf
        </div>
    );
};

export default Footer;